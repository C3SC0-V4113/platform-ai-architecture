# Open Questions

Estas preguntas siguen abiertas o pueden requerir refinamiento futuro.

- El `ai-gateway` se implementara como servicio propio desde el inicio o arrancara embebido en un backend de otro proyecto y luego se extraera.
- Que parte del `asset/document registry` vivira como servicio separado y que parte podria comenzar dentro de un proyecto consumidor.
- Si `admin-console` nace temprano o si `auth-service` + `openclaw-ops` bastan en una primera etapa.
